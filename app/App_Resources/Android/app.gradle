// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "test.native.firebase.way.os"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
