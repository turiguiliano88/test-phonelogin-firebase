// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "nativescript.firebase.phonelogin"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
